import { useCallback } from "react";

export function useAgentStatus() {
  // Placeholder for future extensibility (e.g. local state, animation, etc)
  // For now, no local state is needed, but this hook can be expanded as needed.
  return {};
}
